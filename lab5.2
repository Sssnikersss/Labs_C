//Вариант 1
//При значениях константы "c" = 0.4, и "х" = 3,5 значение "y" должно быть = 1125,45


#define _CRT_SECURE_NO_DEPRECATE
#include <stdio.h>
#include <locale.h>
#include <math.h>


void main()
{
	setlocale(LC_ALL, "RUS");

	double c = 0.4, a, b, y, x;

	printf("Введите значение переменной x:");
	scanf("%lf", &x);
	printf("Введено значение x = %lf\n\n", x);

	a = log10(x);

	b = pow(a, 2) + sqrt(c * x);

	y = exp(2 * x) + pow(9.7, b);

	printf("Ответ:  x = %.1lf\n\ty = %.2lf\n", x, y);
}
